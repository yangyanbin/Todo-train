import React,{Component} from "react";

export default class About extends Component{
    render(){
        return <p className="about">react+Es6+webpack实现的简单的todo list 功能，使用了react中props，state，router，组件等概念，通过es6编码，通过webpack的babel-loader转换为浏览器支持的写法</p>
    }
}